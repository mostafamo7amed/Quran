import 'package:flutter/material.dart';

class CompaseWidget extends StatelessWidget {
  const CompaseWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}